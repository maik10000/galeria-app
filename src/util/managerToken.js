

function  setupTokenExpiration (expires) {
    
    if(expires === 0) return

    const timeUntilExpiration = expires - Date.now()
    
    if(timeUntilExpiration > 0){

        
    }
        
}






export default setupTokenExpiration;